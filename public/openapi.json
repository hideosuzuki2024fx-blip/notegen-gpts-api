{
  "openapi": "3.0.1",
  "info": {
    "title": "NoteGen GitHub API",
    "version": "1.0.0",
    "description": "Public API for NoteGenerator GPTs to access GitHub repository content via Vercel."
  },
  "servers": [
    {
      "url": "https://notegen-gpts-api.vercel.app"
    }
  ],
  "paths": {
    "/api/github/list": {
      "get": {
        "summary": "List files in the NoteGenerator repository",
        "operationId": "listRepoFiles",
        "parameters": [
          {
            "name": "path",
            "in": "query",
            "description": "Optional subdirectory path (e.g., articles/)",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful response with list of repository files",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "name": { "type": "string" },
                      "path": { "type": "string" },
                      "sha": { "type": "string" },
                      "type": { "type": "string" },
                      "download_url": { "type": "string" }
                    }
                  }
                }
              }
            }
          },
          "500": {
            "description": "Server error (GitHub API failure or missing token)"
          }
        }
      }
    }
  }
}
